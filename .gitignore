node_modules/
.svelte-kit/
build/
.wrangler/
worker-configuration.d.ts
.dev.vars
.DS_Store
.env
.env.*
!.env.example
# .env.production holds ONLY the public Turnstile site key (safe to commit; it
# ships in client HTML). NEVER put secrets here — secrets go via `wrangler secret put`.
!.env.production
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# test artifacts
smoke-light.png
.playwright-mcp/
/build/

# verification screenshots (root)
/*.png

# Brainstorm visual companion artifacts
.superpowers/
