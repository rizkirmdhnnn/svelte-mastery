# PRODUCTION build public Turnstile Site Key — your REAL key (safe to expose; ships in client HTML).
# Loaded automatically by `vite build` / `npm run deploy` (production mode overrides .env).
# The matching SECRET is set on the deployed Worker via: wrangler secret put TURNSTILE_SECRET_KEY
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAADgKCxI_8daoYz2B
